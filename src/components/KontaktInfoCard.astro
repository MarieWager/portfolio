---
const { sted, variant, link, kontakt, ani } = Astro.props;
---

<div class={ani}>
  <article class={variant}>
    <h5>{sted}</h5>

    <a href={link} aria-label={`Går til ${sted}`}>
      <h3>{kontakt}</h3>
    </a>
  </article>
</div>

<style>
  div {
    border-radius: 20px;
    article {
      display: grid;
      grid-template-columns: auto 1fr;
      grid-template-rows: auto auto;
      row-gap: auto;

      box-shadow: var(--shadow-drop);
      padding: 20px;

      h5 {
        grid-column: 1 / 3;
      }

      a {
        text-align: right;
        cursor: pointer;
        grid-column: 1 / 3;

        place-self: center end;
      }
    }
  }

  .true {
    min-width: 90px;
    min-height: 90px;

    > * {
      transition:
        transform 0.3s cubic-bezier(0.22, 1, 0.36, 1),
        box-shadow 0.3s ease;
    }

    > *:hover {
      transform: scale(0.97);
      box-shadow: var(--shadow-hover);
    }
  }

  .name {
    article {
      height: fit-content;
      height: 20px;
    }
  }

  .name,
  .gul {
    background-color: var(--sandybrown);
  }

  .lys {
    background-color: var(--lightpurple);
  }

  .lilla {
    background-color: var(--mediumpurple);
  }

  .name,
  .gul,
  .lys,
  .lilla {
    border-radius: 20px;
    width: 100%;
    height: 100%;
  }
</style>

{
  /*

div {
    border-radius: 20px;
    width: 100%;
    height: 100%;
    article {
      display: grid;
      grid-template-columns: auto 1fr;
      grid-template-rows: auto;
      row-gap: auto;

      box-shadow: var(--shadow-drop);
      padding: 20px;

      width: 100%;
      height: 100%;
      min-width: 90px;
      min-height: 90px;

      a {
        text-align: right;
        cursor: pointer;
      }
    }
  }




*/
}
